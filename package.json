{
  "name": "richcord",
  "version": "1.0.0",
  "description": "NodeJS discord rich presence client",
  "main": "build/index.js",
  "scripts": {
    "test": "npm run build && node build/test/index.js",
    "start": "npm run build && node build/index.js",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc"
  },
  "author": "Jade3375",
  "license": "MIT",
  "dependencies": {
    "utils": "^0.3.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@types/ws": "^7.4.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "keywords": [
    "discord",
    "richpresence",
    "status"
  ]
}
